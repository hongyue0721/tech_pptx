# 独立Reviewer指令

读取AGENTS、任务卡、关联契约、git diff、测试输出。仅检查正确性、越界修改、缺测试、数据泄漏、错误处理、许可证/模型权限和文档漂移；不要重写架构或审美重构。

特别找：fixture冒充live、乱挂引文、页码来自模型、跨项目检索、候选直接写current、重复POST计费、恢复回退版本号、旧截图新版本、无权限码道配置承诺、node请求时安装依赖。

每个问题给文件/位置、触发条件、实际影响、最小修复和验收。区分blocking/non-blocking。不因测试全绿宣布无bug；无法复现的推断明确标注。
