# process｜当前工程事实与交接

版本：1.0.21；日期：2026-09-20；阶段：**M3 / T11 前端 F2 完成（真实资料→大纲→候选全链真实模型实测 PASS；prompt 三处生产错位修复 content-v3/verify-v2/audit-v2，527 全绿）；远端 6f58642，本地多 commit 未 push**。

## 当前事实

规划包已入库（commit 8e30020）。T00 由负责人完成。**2026-09-18 晚 T01/T02 两项待验收全部闭环：** ① 负责人实测 WPS/PowerPoint 打开 `minimal_probe_v2.pptx` 成功，改文本框→保存→重开修改仍在（T02 L2/L3 硬验收 PASS，真实可编辑文本而非截图冒充）；② 沙箱网络恢复（宿主 clash TUN fake-ip 已覆盖沙箱子进程），`codearts models` 列出全部三 provider 共 10 模型（huaweicloud-maas 4 + hyper 2 + ollama 4），`codearts run -m ollama/glm-5.3-flash` 真实调用模型回复"探针OK"（T01 模型直连 PASS）。**M0 硬门禁（T01/T02 不通过不进 T03）已通过。**历史：v1 PPTX 打不开（缺 slideMaster/theme 链）已修复出 v2（10部件），v1 留反例。沙箱内 WPS 无头验证不通（Xvfb 下 GUI 进程裸启动即退，如实记录）。CLI 26.9.3 / 认证 / 会话导出此前已 PASS。（以下为 **2026-09-18 时点事实，现已过期**：当时尚未实现 Courseware Copilot 应用、未创建 ECS、未向 GitHub/AtomGit 写入、未调用收费模型。截至 2026-09-20：backend 应用 T03–T09+F00 已实现并推送至 GitHub 公开仓库 tech_pptx master=6f58642；仍未创建 ECS、未公网部署、未调用收费模型——真实模型黄金链路 APP_LIVE=NOT_RUN。）

| 对象 | 状态 | 证据/说明 |
|---|---|---|
| 规划/接口/工程规范 | DOCS_CREATED | 本文档包，校验见validation-report.md |
| Demo教材、案例与负向输入 | DATA_CREATED | demo-data清单与解析检查；不是产品生成结果 |
| T00 资格/账号/授权/预算 | DONE | 负责人确认；本机开发；不外发/不付费/不公网 |
| 真实CodeArts账号/模型权限 | OWNER_CONFIRMED | T00 负责人确认；2026-09-18晚 CLI AK/SK 已实测可用 |
| CodeArts外接模型工具调用（会话内） | PASS | T01探针：会话内8项中7项PASS；故意失败真实修复PASS；证据smoke/ |
| 独立码道CLI进程 | PASS | HOME重定向+AK/SK后 --version/session list/export/models/run 全部实测PASS（26.9.3）；2026-09-18晚网络恢复后 models 列出10模型、run 真实调模型回复"探针OK" |
| 会话导出（T01验收项） | PASS | `codearts export <id>` 导出 JSON 成功、脱敏扫描通过；补充探针 S4 |
| 原创Skill运行 | NOT_RUN | teacher-courseware 未实现（T13），系统级内置Skill在线（listSkills实测12个） |
| AnyUI安装/构建 | **PASS** | 正确坐标 `@any-design/anyui@0.5.2`（npm裸名anyui是2017无关Angular包=供应链陷阱）；--legacy-peer-deps+仅Vue peers无React/Svelte混入；vite7.3.6构建PASS(120 modules)；Playwright运行时AInput/AButton/ADrawer/Toast四组件真实渲染交互（smoke/t02_anyui_probe/anyui-install-test/） |
| pypdf中文抽取链 | PARTIAL_PASS | 8页/2701字符全中文；reference_chunks 12/12 偏移+sha256一致；版本差异6.19.0 vs 锁定5.9.0已记录 |
| 最小可编辑PPTX探针 | **PASS** | v1打不开（缺master/theme链）→v2修复（10部件完整链）→**负责人WPS实测：打开+改文本框+保存重开修改均在**（L1-L3全过） |
| ppt-edit授权/稳定性 | OWNER_CONFIRMED | T00 负责人确认授权门禁；具体结论见private-evidence（不入库） |
| T03 Schema/API与工程骨架 | **DONE** | 已随 commit 18f4f6c 入库。backend：Pydantic v2 44类型对齐models.schema.json（**独立review发现items元素级约束缺口15处，已按"先失败用例后修复"闭环，双向契约验证成立**）+ FastAPI health/projects CRUD + 统一错误形状（含路由级404/405收口）+ X-Request-ID + Idempotency-Key门禁；frontend：Vue3+TS strict+Router两路由+api client，vue-tsc+vite构建PASS；review报告 docs/reviews/t03-review.md |
| T04 SQLite任务/幂等/版本仓库 | **DONE** | 已随 commit b5cf4da 入库。jobs表+JobRepository（单语句CAS领取、终态与项目锁同事务释放、cancel全CAS防竞态）；JobWorker单例（flock锁文件、启动恢复running→interrupted不重放、handler异常→failed）；幂等24h+占位行防双执行（同键同请求重放、异请求409）；项目写锁409 PROJECT_BUSY；VersionRepository服务端权威版本号+semantic_hash；ArtifactStore原子落盘后写指针；独立Review 1阻塞+14非阻塞修复闭环；报告 docs/reviews/t04-review.md |
| T06 检索/覆盖/证据定位 | **DONE(Review修复闭环)** | retrieval/tokenizer（jieba 中文词≥2字 + ASCII 标识符整词小写，TOKENIZER_VERSION 固化）；retrieval/bm25（BM25Plus 防小语料负 idf 全滤、精确词命中加成、**revision=累积快照 `<=` 语义**、无结果返回空不塞默认教材）；evidence/locator（仅当前项目累积语料、quote 唯一连续子串、0次=不存在/多次=歧义拒绝、document/page/偏移全部服务端填充、EVIDENCE_INVALID 422）；coverage_service（逐目标、最佳子串覆盖率 supported≥2/3/partial≥1/3/否则 unsupported，规则层不假装能判 conflict）；变体端到端不特判、页码随变体物理页；evaluation/cases.json 检索命中率**实测 10/10**（测试侧读取不进运行路径，底线 0.8）；期间修正 revision 语义错误预期（累积快照非仅新增）与虚构目标（教材无 EXTI，unsupported 才正确）；pytest 310全绿；**独立Review 1阻塞(B1 BM25Plus 常数项混入零交集片段)+8非阻塞：B1/N1/N2/N3/N4/N6 修复、N5/N7 注释与自洽断言固化，315全绿**；报告 docs/reviews/t06-review.md |
| T07 APP模型Adapter与预算 | **DONE** | courseware_core/llm/ 四模块：config（LLMConfig 从 APP_LLM_* 环境变量读取、协议白名单只认 chat_completions、repr/str 永不携带 api_key、能力位 supports_temperature/supports_json_mode 从 env 读取默认 true）；budget（CallBudget 按实际HTTP请求计数含重试与修复、单线程假设注释固化、JobContext deadline(monotonic)/cancel_check/ensure_active，project/revision/trace 留 T08 补齐）；adapter（ChatCompletionsAdapter.complete_json(stage, schema_name, messages, context)→TypedCompletion(value/usage/provider_request_id/attempts)：POST {base}/chat/completions + Bearer + response_format json_object（能力位可关）+ temperature 能力位可关；typed JSON=OUTPUT_SCHEMAS 注册表（PlanProposal/SemanticVerdicts，模型提案类型不混存储类型）+ pydantic 校验 + **一次**带校验错误的修复（原消息+assistant坏输出+user修复指令）；重试矩阵：429/500/502/503/504/超时/连接错误→**整个调用内至多一次**退避重试（Retry-After 封顶5s、非数字回落0.5s、无则0.5s），401/403→ModelAuthError、400/404等→ModelConfigError 均不重试，重试耗尽→ModelUnavailable；200+非JSON体→ModelOutputInvalid（B1）；usage 缺失/字段缺失→None=unknown 不写0；预算耗尽→BudgetExceeded（修复请求也被预算拦截）；取消位/deadline 在发请求前、退避sleep后、return前检查→JobCancelled/JobDeadlineExceeded）；测试 38 条全 MockTransport 注入零真实网络；**独立Review 1阻塞+7非阻塞：B1/N1/N3/N4/N6/N7 修复闭环、N2/N5 注释固化进 T08**（报告 docs/reviews/t07-review.md）；**真实协议 smoke（负责人预算授权后实测，报告 docs/t07-protocol-smoke.md）：deepseek-flash Chat Completions 六探针全 PASS——中文文本/JSON mode+temperature=0/错Key→401/未知模型→400（非404）/adapter端到端 attempts=1 usage known/取消位零请求拦截；能力位实测 supports_json_mode=true supports_temperature=true 回填 .env 默认；429 Retry-After=NOT_TRIGGERED、超时=NOT_RUN（对应逻辑仅单测背书，如实声明）**；pytest **353全绿**。**T08 轮修正**：发现 api.md 实际存在（规划包根目录，错误码表真源，此前“没有api.md”的结论作废），T07 错误码按 api.md 表对齐重构：MODEL_UNAVAILABLE 拆为 MODEL_RATE_LIMIT(429)/MODEL_TIMEOUT(超时与deadline)/MODEL_UNAVAILABLE(5xx/连接)；MODEL_CONFIG_ERROR 改 MODEL_PROTOCOL_ERROR(400/404/本地配置缺失)；unknown schema 归 VALIDATION_ERROR；JOB_CANCELLED 改 CANCELLED；JOB_DEADLINE_EXCEEDED 并入 MODEL_TIMEOUT；MODEL_OUTPUT_INVALID/MODEL_UNAVAILABLE 两码 api.md 表暂缺（已请示负责人补行）。错误码 HTTP 映射已在 T08 接入 error_mapping |
| T09 候选生成、语义核验与提交 | **DONE（Review闭环，已提交并 push 6f58642）** | ① changes 表（v3 迁移，整对象 change_json+status/kind 冗余列 CHECK 对齐 schema enum）+ChangeRepository（create_generate_job 占锁建 job+锚点同事务；mark_committed_tx 单一 CAS SQL，列与 JSON 同语句同步）+GET change（跨项目 404 不泄露存在性）；② GenerateService.create_generate_job（consent 门禁+消费 R00-C 精确接缝+base_version 前置+幂等锚点）与 handle_generate（每批≤3页 ContentProposal→resolve_evidence 转存储 Claim+locator 失败一次修复（prior 上下文、final_proposal 同版本回传）→批语义核验（invalid claim 不进模型队列、零 fact 不调模型、漏答/重复 verdict=not_checked）→关系/layout 门（页 id 集合==计划集合）→can_commit 可执行门（docs/04:41）→CandidateChange ready/blocked 落库；jobs.params_json 内部列（v4 迁移）；retrieval/context.py 公平预算装配提共享（plan 重构复用）；GENERATE_CALL_BUDGET=24、stage retrieving/generating/validating、execution_guard 复用）；③ commit_change（stale 读路径计算+根因分流 CorpusChanged/VersionConflict、blocked/committed/核验未过→CHANGE_NOT_COMMITTABLE 409、acknowledged Literal[True] 强制；commit_version on_committed 回调="版本指针 CAS 前进⇔候选 committed"同事务原子，并发双 commit 恰一方成功 Barrier 竞态背书）；POST /generations、POST /changes/{id}/commit 路由（幂等+scope 含 change_id）+wiring generate_handler+api.md 补 CHANGE_NOT_FOUND/CHANGE_NOT_COMMITTABLE 两行；测试 54 条（unit 39+contract 15，含端到端 confirm→generate→GET change ready、崩溃锚点恢复、并发提交）；独立 Review 2 阻塞（B1 修复路径核验A展示B、B2 重复 verdict 假绿）+6 非阻塞全当场红→绿闭环（回归均验证旧实现必红）；pytest **478 全绿**、validate_pack 23/23、build 绿；报告 docs/reviews/t09-review.md。**未做：semantic 自动修复循环（unsupported→blocked 收口，教师补料再生成；docs/06:37 兜底路径，自动改写进 backlog）、前端消费（T11）、真实模型黄金链路（T14）** |
| T09-B 可信边界收口 + F00 只读接口 | **DONE（UNIT/MOCK 背书，待独立 Review 后提交）** | 按 AGENT_00 Q01–Q08 五循环 TDD 先红后绿：①（Q01/Q02/Q03/Q06）独立可见文字审计通道 `audit_visible_text`/`VisibleTextAudit`（app-prompts/audit.md v1）——零 claim 批不再跳过审计、豁免判据=教师计划 layout=title（模型自报 title 不豁免）、audited_slide_ids 与必审页集合服务端双射核验（遗漏/重复/未知页整批不采信）、assumptions 文本入审计输入；missing_evidence 非空 P0 保守 blocked（放宽须先写 ADR）；verdicts 与预期 claim 集合严格双射（造新 ID=整批 not_checked）；②（Q04/Q05）提案/存储类型隔离：`ProposalSlide`/`ProposalIllustrationBlock`（evidence_refs 只允许 chunk_id+quote，document_id/pdf_page/start/end 服务器权威字段 Schema 层拒绝）、illustration 引用全部经 locator 转换、失败引用不入库（空 refs+报告+blocked）；resolve_evidence 新增必填 `allowed_chunk_ids`=本批实际进 prompt 片段集合（项目存在但未提供=不可引用，claim/illustration 两路径）；**修复真实 adapter 生产路径实锤 bug：OUTPUT_SCHEMAS 缺注册 ContentProposal（ScriptedProvider 掩盖，MockTransport 真实路径测试暴露）**；契约同步 models.schema.json+openapi.json+samples（4 新类型）；③（Q07）教师确认顺序与布局保护：批内乱序由服务器按计划确定性重排落库（不浪费模型调用、不乱序进教师视野）；layout 门扩展"每页 layout==计划 layout"（偷改 layout→layout_valid False→blocked）；结构检查不冒充 PPT 渲染检查（预览语义归 T10）；④（Q08）候选写库前 `guard_writable`（DB 实况复查 running+未取消+deadline 未过；取消/超时/被标 interrupted 落在"最后核验→写库"窗口=JobCancelled/ModelTimeout 且 changes 表零写入，不再只依赖 worker publish 复核）；⑤（F00）GET /projects/{id}/deck?version=N（缺省=current_version、0/缺失/跨项目→404 DECK_NOT_FOUND）与 GET /projects/{id}/evidence/{chunk_id}?corpus_revision=N（累积 revision 内、超前 409 CORPUS_CHANGED、跨项目/超范围 404 EVIDENCE_NOT_FOUND 新码入 api.md/error_mapping，response_model 契约化）；`courseware_api/dev.py` 开发启动装配（parse/plan/generate handlers+唯一 worker+只绑 127.0.0.1，create_app(worker=None) 测试工厂不再被当作业务服务）；测试 478→**524**（净增 46：审计门 11+提案隔离/allowed 7+顺序布局 3+写守卫 3+F00 契约 12+dev 装配 2+locator 1+注册表 1+多批 missing 反例 1+契约样本等），validate_pack 23/23。**独立 Review（干净会话子代理，模型 huaweicloud/qwen3.8-flash）：0 阻塞+8 非阻塞，Q01–Q08/F00 逐项判定 CLOSED、总体可提交；当场闭环 N1（docs/05 L2 与 docs/04 §19 补 allowed_chunk_ids/提案隔离语义）、N2（api.md deck 行注明 version<1→404 语义）、N5（重排加 len 条件，重复 id 不去重不静默丢页）、N6（守卫反例补 llm_calls 累计断言）、N7（openapi 末尾换行）、N8（多批 missing_evidence 反例）；进 backlog：N3（generate_service 648 行>500，门校验外置时机）、N4（guard_writable 通过后→写库→publish 复核间极窄竞态可留 ready 孤儿候选——彻底闭合需 job↔change 同事务或关联列，P0 下教师无路径获知孤儿 id 且 L4 人工门禁兜底，是否本轮收口待负责人拍板）。**未做：T10/T12/云部署/前端大改；APP_LIVE=NOT_RUN（无预算授权，不以 Fake 数字补证据）** |
| T08 大纲生成与教师确认 | **DONE（Review闭环，commit 444209e+f6f860a+c126edd+3efdc84）** | ① plans 表（plan_json 真源 + status/corpus_revision 冗余列 + 四态 CHECK + FK CASCADE，对齐 deck_versions 存法）+ PlanRepository（create_with_job 占锁建 plan job 单事务，与 T05 同语义）；② PlanService：create_plan_job（corpus_revision 必须=项目当前值否则 CORPUS_CHANGED，无材料 corpus=0 天然拒绝）；handle_plan（worker 内：evaluate_goal_coverage 服务端计算为真源、模型自评 coverage_notes 忽略不采信；全目标 unsupported 先抛 InsufficientEvidence 不烧模型调用；按 docs/05 组装上下文——每目标≤8片段、全局8000字符、轮转取片段防后目标饿死；provider.complete_json("plan_course","PlanProposal")；slide 的 evidence_chunk_ids 必须∈本次检索命中集合否则 ValidationFailed；status=needs_material(有缺口)/draft、accepted_goal_indices 默认仅 supported+partial、id/project_id/corpus_revision/created_at 全部服务端填；llm_calls 累计 jobs 列）；confirm_plan（缺口目标混入 accepted→ValidationFailed 含 gap_goals；slide 触及未接受目标→拒；chunk 引用∉项目语料→拒；语料前进→stale 拒确认 CORPUS_CHANGED；request.revision≠plan→CORPUS_CHANGED；已确认幂等重放；确认后 status/slides/accepted 取教师版）；stale 读取路径计算不写库（材料入库零耦合）；③ blocked 收口（api.md:59 资料不足=blocked+INSUFFICIENT_EVIDENCE 与 failed 严格区分）：_TERMINAL_STATUSES 加 blocked（终态、释锁、不重放）、worker 加 InsufficientEvidence 分支、_error_response 带 details；④ JobCancelled 双类统一（T04 worker 局部类与 T07 DomainError 版重名不同类会让取消误归 failed——本轮发现并合并到 errors.py 单一来源）；⑤ T04-N4 backlog 闭环：plan handler cancel_check 轮询 DB 取消位；⑥ routes_plans 三路由（POST plans 202/GET plan/POST confirm；confirm 幂等 scope 含 plan_id）+ error_mapping 扩展（PLAN_NOT_FOUND 404、INSUFFICIENT_EVIDENCE 422、MODEL_* 502/503 组、CANCELLED 409）+ wiring plan_handler（APP_LLM_* 惰性构造真实 adapter，测试注入 Fake 走同路径）；测试 30 条（unit 21 + contract 9）；Review 后 **389 全绿**（基线 commit 3efdc84）、validate_pack 23/23、frontend build 绿。**未做：前端（T11）、真实模型黄金链路 plan（T14）、usage/请求日志落库（待 api.md 补行决策）**。P0风险登记（T08-N10）：教师教材文本可含prompt注入指令，缓解=Schema校验+引用须∈实际进prompt子集+教师门禁，语义防护归T09/T15 |
| T05 独立Review | 1阻塞+16非阻塞 | **B1（配额满时 duplicate 被误拒413，去重上移先于配额）+ N1(Content-Length预检)/N2(EXTRACTION_LIMIT_EXCEEDED独立码)/N3(非DomainError兜底failed不留僵尸)/N4(启动孤儿GC)/N5(failed不算duplicate)/N6(chunk对象载荷去双源)/N11(指纹长度前缀)/N13(failed释锁断言+e2e失败链)/N14(限额单一事实源)/N15(注释) 修复闭环，pytest **276全绿**；backlog：N7(预算口径已核)、N8(PageLabels差距未实现，T06前记录)、N10(矢量/Type3页误判EMPTY_PAGE，T14变体覆盖)、N12(duplicate等待把手=轮询GET materials)、N16(解析超时/子进程隔离，T15)；报告 docs/reviews/t05-review.md |
| T05 PDF导入/规范化/切块 | **DONE**（commit 2a05115） | pypdf 锁定版 **5.9.0 对账闭环**（reference_chunks 12/12 偏移+hash 一致，固化为回归测试）；materials/pages/chunks 三表；normalize 固定归一化（CRLF/CR→LF、NFC、删NUL、整页strip、不折叠空格）；parser 逐页抽取+typed 异常（PDF_ENCRYPTED/UNSUPPORTED_FILE/PDF_TEXT_UNAVAILABLE/PAGE_LIMIT_EXCEEDED/EXTRACTION_LIMIT_EXCEEDED）+每页警告（SCAN_DETECTED 用 XObject 检测不依赖 Pillow/EMPTY_PAGE/PAGE_PARSE_ERROR）；chunker 段落贪心+句级切分、目标≤900 硬上限1200、P0 零重叠、偏移自洽；MaterialService 限额(20MiB/5份/100MiB/200页/50万字符)+SHA去重+文件名转义+原子落盘+**占锁建job同事务（N1收口）**；claim_next 校验写kind持锁（N8收口）；POST/GET materials 路由+multipart 幂等+worker 端到端测试；pytest 270全绿 |
| 应用真实LLM/检索/版本/E2E | NOT_RUN | 无业务实现，不伪造测试结果 |
| 校内截止/命题参与数/书面认定 | OWNER_CONFIRMED | T00 负责人确认 |

## 当前任务
**T11 前端 F1 完成，下一步 F2（资料+大纲真实接口链路）（2026-09-20）**。F1 交付：`@any-design/anyui@0.5.2` 锁版本正式接入（`@any-design/anyui/vue` 入口+styles/index.css，Vue peers 显式声明，--legacy-peer-deps 为 T02 smoke 已验证的特定 peer 冲突处理）；ADR-10 登记三业务视图替代旧两路由限制；路由 `/`、`/project/:id/{materials,outline,review}` + `/project/:id` 兼容转入；CourseShell（62/60/1fr/66 固定外壳、阶段导航 aria-current、底部主操作）；IntakePage（创建前真实表单+本地待提交队列、创建后真实 GET project 只读+真实 GET materials 列表、未接上传诚实 disabled）；OutlinePage（真实 GET plan 三栏渲染、coverage 检索语义文案、无 plan 参数空态不猜最新）；ReviewPage（真实 GET change/deck、候选/正式状态标签服从服务器、结构预览标注非 PPT 渲染、导出/应用诚实 disabled+原因、<1080 Inspector 抽屉）；契约类型 types/models.ts 全量对齐 models.schema.json；api/resources.ts 域模块。验收：`npm run typecheck`+`npm run build` 退出码 0；截图 docs/screenshots/f1/ 7 张（1366×768 三页、941×768 review、1440×900、1180×740）；无 JS 异常（favicon 已补本地 SVG）。生产代码路径零 mock 数据。上一轮 T09-B 已收口（524 全绿、f41c8f6+0b36f77+文档段）。**F2 目标**：队列逐份上传+parse 轮询+plans/confirm→generations 串行+真实端到端浏览器链。T10 导出器选型仍待哥哥拍板（不阻塞 F1-F3）。

## 新增阻塞（负责人决断）

- **B-1 外网DNS不可达（沙箱内）**：~~已解除~~ **2026-09-18 晚实测已解除**——宿主 clash TUN fake-ip 已覆盖沙箱子进程，npmjs HTTPS 200、CLI 模型直连成功。AnyUI 实装与在线锁版本不再被阻塞。
- **B-2 独立码道CLI EROFS（已闭环）**：HOME 重定向到可写目录后 CLI 全功能可用；models/run 已实测 PASS，阻塞清零。
- **B-3 PPTX Office验收（已闭环）**：**负责人 2026-09-18 实测通过**——打开 v2 成功、改文本框、保存重开修改仍在。T02 验收完成。

## 本轮实际检查

- 码道会话内 smoke：读文件/改文件/建测试/故意失败/真实修复/回归，退出码探针 3 组全对。命令与结果见 `smoke/t01_codearts_probe/`。
- 构建探针：`smoke/t02_anyui_probe/vue-offline-test/`（vite build 真实产物 dist/；离线搭建脚本 setup_offline_node_env.sh 已固化）。
- PDF 探针：`smoke/t02_pdf_probe/extract_report.json` + `chunk_verify.py`（12/12 一致）。
- PPTX 探针：v1（2050字节）负责人实测**打不开**→根因（缺 slideMaster/slideLayout/theme，OPC 仅 2 部件）→ v2 修复（4909字节 10 部件完整链，构建脚本 /tmp/pptx_fix/build_full_pptx.py 逻辑固化在探针目录）→ xmllint 良构 + 关系闭合审计 + python-docx OPC 解析（5部件/7关系集）+ Playwright 1:1 布局渲染（标题四行正文完整无重叠）四重验证通过；v1 改名 minimal_probe_v1_BAD.pptx 留反例。
- 沙箱内 WPS 无头验证尝试失败（Xvfb 下 /opt/kingsoft/wpp GUI 进程裸启动即退，多轮复现），此路不通，如实记录不作为证据。
- CLI 补充探针（2026-09-18 晚，负责人 AK/SK）：`--version`=26.9.3、`session list`=7 条、`export`=JSON+脱敏通过；`run -m ollama/glm-5.3-flash` 认证/配置解密通过但沙箱内 ConnectionRefused，关键结论已写入 smoke 报告。
- **闭环探针（2026-09-18 20:49，网络恢复后沙箱内实测）**：`codearts models` PASS（huaweicloud-maas 4 + hyper 2 + ollama 4 共 10 模型）；`codearts run -m ollama/glm-5.3-flash "只回复三个字：探针OK"` PASS（模型真实回复"探针OK"）。T01 模型直连闭环。
- **T02 Office 验收（负责人 2026-09-18 实测）**：WPS/PowerPoint 打开 minimal_probe_v2.pptx 成功，改文本框→保存→重开修改仍在。T02 L1-L3 全过。
- **T03 施工（2026-09-18 21:00–21:30）**：`backend/` uv 建 Python 3.12.14 venv + uv.lock（fastapi 0.141.1/pydantic 2.13.5/httpx 0.28.1/uvicorn 0.53.0/pytest 8.4.2/jsonschema 4.26.0）；`courseware_core/models/` 44 个 Pydantic 类型与 models.schema.json 对齐（extra=forbid、const→Literal、oneOf→discriminated union、datetime 强制带时区）；`courseware_api/` health+projects 三端点、统一错误形状、X-Request-ID 验证/回传/生成、Idempotency-Key 存在性门禁（完整幂等语义留给 T04）；`frontend/` Vue3+TS strict+vue-router 两路由+api client+useProject，vue-tsc+vite 构建 PASS。测试：`.venv/bin/pytest` **119 passed**（契约往返 44+extra拒绝 44+非法样例 14+naive datetime+API 行为+service 单测），退出码0。
- **T06 施工（2026-09-19）**：按 docs/05 落地检索与证据定位。① `retrieval/tokenizer.py`：jieba 中文词（过滤单字防跨主题误命中）+ ASCII 标识符整词小写保留（NVIC_SetPriority 不切碎，Cortex-M4→cortex/m4），TOKENIZER_VERSION=jieba-0.42.1+ascii-v1；② `retrieval/bm25.py`：BM25Plus（Okapi 单文档小语料 idf 为负会把候选全滤空，命中率实测 30% 暴露后切换）、完整查询短语+ASCII 词面精确命中加成、无结果返回空；**关键语义修正**：revision 查询从 = 改为 <=——corpus_revision 是截至该版本的累积语料快照（材料入库不删除），命中率修复到 10/10；③ `evidence/locator.py`：chunk 必须属于当前项目累积语料，quote 必须唯一连续子串（0 次拒绝、多次 ambiguous 拒绝不取第一次），偏移/页码/document_id 全部服务端填充；EVIDENCE_INVALID→422；④ `services/coverage_service.py`：逐目标检索，最佳子串覆盖率（jieba 边界不对称使词形交集漏判配置流程，改子串匹配）≥2/3=supported、≥1/3 或单候选=partial、否则 unsupported；note 表述为当前材料检索未找到足够支持，不断言知识不存在；conflict 留给 T09 语义层；⑤ 变体端到端：renamed_reordered_notes.pdf 改名+重排后同路径命中、页码自洽于变体物理页，无文件名/特定词特判；⑥ 命中率指标：evaluation/cases.json retrieval 类 10 条 Top8 实测 10/10（断言底线 0.8，数字打印入测试输出）；⑦ 修正 3 处错误测试预期（revision 隔离方向、EXTI 目标虚构——教材实无 EXTI 内容，unsupported 才是正确行为）。测试：净增 34 条（tokenizer 7/bm25 9/locator 9/coverage 5/variant 3/metrics 1），pytest **310 passed** 退出码0。诚实声明：命中率 10/10 是演示语料小样本实测，不宣称泛化；BM25 每次查询现建索引，规模缓存留给后续。
- **T05 施工（2026-09-19）**：按 docs/04+docs/11 落地 PDF 导入链。① **pypdf 5.9.0 锁定版对账闭环**（T04 遗留前置项）：uv 装 pyproject（pypdf==5.9.0+python-multipart），reference_chunks 12/12 偏移+hash 与 6.19.0 探针结果一致，固化为 `test_reference_chunks_reconcile.py`（含 quote 页内唯一性）；② `materials/normalize.py` 固定归一化（改算法必须提升 extractor_version=`pypdf-5.9.0-nfc-v1`）；③ `materials/parser.py`：magic bytes 结构校验（不只扩展名）、加密拒绝不解密、逐页抽取+归一化、扫描页检测用 XObject /Subtype=/Image（page.images 依赖 Pillow 栈外，不用）、混合文件逐页 SCAN_DETECTED/EMPTY_PAGE 告警、全空文档 PDF_TEXT_UNAVAILABLE、单页失败 PAGE_PARSE_ERROR 不中断整本、页数/字符限额 typed 异常；④ `materials/chunker.py`：段(

)贪心至900、超长段句级(。！？；)切分、单句超1200硬切、P0 零重叠（重叠上限100取0，偏移自洽优先）、chunk.text==归一化页[start:end]、短页不拼页；⑤ `MaterialRepository.create_with_job` 占锁+material+job 单事务（**N1 收口**）、`save_parsed` corpus+1 与 pages/chunks 落库单事务、失败整体回滚不推进 corpus；⑥ `claim_next` 联表校验写 kind 必须持锁、export 只读可排队（**N8 收口**，T04 测试同步更新）；⑦ `MaterialService.upload`：20MiB/5份/100MiB 限额、SHA 去重返回原 material 不增 revision、文件名 sanitize（Path.name+可打印过滤，绝不参与路径）、原子落盘 tmp+fsync+rename+目录fsync；⑧ 路由 POST multipart 202（幂等指纹=路由+文件名+文件hash）/GET MaterialList；`wiring.build_worker_handlers` 每 handler 独立连接；⑨ 修复：async 路由+同步 DB 依赖跨线程 ProgrammingError → 同步路由 file.file.read()。测试：净增 57 条（normalize 7/parser 11/chunker 8/material_service 16/API materials 9/reconcile 4/N8 锁校验 2），含 worker 端到端 upload→succeeded→ready+corpus=1，`.venv/bin/pytest` **270 passed** 退出码0；validate_pack 23/23；build 不回归。诚实声明：真实扫描型 PDF 的 SCAN_DETECTED 判定基于图片 XObject 存在性，未做视觉确认（P0 不 OCR 不承诺）。
- **T04 施工（2026-09-19）**：按 docs/07 落地可靠状态层。① `storage/database.py` 新增 jobs/idempotency_keys/deck_versions/artifacts 四表（状态/stage CHECK 与 schema enum 对齐，FK 到 projects）；② `JobRepository`：单语句 `UPDATE...RETURNING` CAS 领取、mark_interrupted_on_startup（running→interrupted 且同事务释放项目锁）、finalize（终态+释放锁同事务）、request_cancel（queued→cancelled / running→置标志）；③ `jobs/worker.py` JobWorker：flock 锁文件单实例（第二实例 WorkerAlreadyRunning）、启动恢复不重放、线程内独立连接、handler 异常映射 failed（DomainError 保留 code）；④ 幂等：`IdempotencyRepository`+`IdempotencyService`（scope=会话|项目|路由，占位行 INSERT OR IGNORE 防双执行，24h 过期重执行，同键异请求 IdempotencyConflict→409），`api/idempotency.py` 接入 POST /projects、DELETE /projects、POST /jobs/cancel（T03 存在性门禁升级为完整语义）；⑤ `ProjectRepository.acquire/release_write_lock`（CAS，holder 匹配才释放）；⑥ `VersionRepository`：服务端权威版本号（候选 version 被覆盖）、base/corpus CAS 409、restore=新 version 且异 corpus 拒绝、semantic_hash 忽略 version；⑦ `ArtifactStore`：tmp+fsync+os.replace 原子落盘后才写指针，读取校验 sha256/size 不一致 ARTIFACT_INTEGRITY 显式失败，artifact_id 路径穿越拒绝；⑧ `create_app(worker_handlers=…)` lifespan 启动单 worker（默认 None 不启用）；⑨ 500 路径故障注入测试（T03 backlog N3 闭环：X-Request-ID 在 500 响应头与错误体均保留）。测试：新增 66 条（job_repo 15/lock 6/idempotency 7/version 8/artifact 9/worker 7+API jobs 7+API idempotency 5+500 注入 2），`.venv/bin/pytest` **206 passed** 退出码0；validate_pack 23/23；frontend build 不回归。诚实声明：LLM/渲染 handler 未接（T05+），worker 执行路径用注入 handler 验证，无真实业务 job 类型可跑。
- **AnyUI 实装闭环（2026-09-18 晚）**：见 smoke 报告"AnyUI 实装闭环"节——`@any-design/anyui@0.5.2` 安装/构建/运行时四组件全 PASS；npm 裸名 anyui 为无关旧包（供应链陷阱，已记录禁用）。
- **校验器适配（经负责人批准，tools/ 越出 T03 allowed_paths 的唯一例外）**：`tools/validate_pack.py` 三处全文件系统 rglob 改为 **git 分发视角**（`git ls-files --cached --others --exclude-standard`），PACK_STATIC 从此只验"将被分发的内容"，node_modules/.venv 等 gitignore 目录自然排除；顶层禁 node_modules/data/private-evidence 与字体检查规则不变。改后回归 **23/23 PASS**。
- **T03 独立 Review（干净会话，只审改前）**：发现 3 阻塞（B1 items 元素级约束丢失 15 处、B2 测试盲区、B3 路由级 404/405 错误形状未统一）+ 7 非阻塞。修复严格按"先失败用例（19 条 items 用例先红，失败点确认在 Pydantic 侧）→ 修实现 → 转绿"；B3 加 StarletteHTTPException handler + 2 条用例；N1 错误体经模型构造、N2 encodeURIComponent、N4 .gitignore 补 .playwright-mcp、N5 401 独立状态。N3（500 路径故障注入）、N7（schema enum 缺 type:string）进 backlog。报告与闭环记录：`docs/reviews/t03-review.md`。
- 最终回归：`pytest` **140 passed**（退出码0）；`validate_pack.py` 23/23；frontend `npm run build`（vue-tsc+vite）PASS。
- 环境落地：规划包 `.venv`（Python 3.13.15 + pypdf/jsonschema/PyYAML/pytest 全离线装齐）已建，`validate_pack.py` 回归 **23/23 PASS**（此前"jsonschema 全机无安装"的结论已由实测推翻并闭环）。
- **T09 施工（2026-09-20，基线 069083b，三循环 TDD 先红后绿）**：详见"当前任务"节与 docs/reviews/t09-review.md。测试 424→**478**（净增 54：changes repo 6+契约 3+生成单测 22+契约 12+commit 8+Review 回归 4+预热 fixture 不计数；并发用 Barrier+临时 SQLite，崩溃注入在契约层）。Review 发现并修复的实质缺陷：B1 `_locate_batch` 修复分支不回传 proposal→落库/核验用修复前页面（"核验A展示B"，同构夹具掩盖，差异化标题回归破）；B2 重复 verdict 保留首条与注释相反→supported/unsupported 冲突被盖绿（pop+dup→not_checked）；N2 layout 只比页数可被偷换页 id 过门；N3 越界产出裸 ValidationError→INTERNAL_ERROR（改 typed ModelOutputInvalid）；N4 commit 内联 SQL 与 mark_committed 平行双源（合一 tx 方法）；N1 samples SAMPLES 重复键新样本成死代码。jieba 冷启动 flake 治本=conftest session autouse 预热（e2e 等待窗 10s 未再放宽）。门禁：`pytest backend/tests` **478 passed 退出码0**（连跑2次稳定）；`validate_pack.py` **23/23**；frontend build 退出码0。诚实声明：真实模型 generate/verify 黄金链路 **NOT_RUN**（授权不覆盖；语义遵从 T14 实测）。
- **R00 边界收口（2026-09-19 深夜–09-20，基线 3efdc84，四组补丁 TDD 先红后绿）**：**A** consent 门禁（受理+执行双检、consent=false 零 provider 调用、CONSENT_REQUIRED→409 补 api.md 行）+ Prompt 唯一事实源 `app-prompts/plan.md` v2（`llm/prompts.py` 版本化加载、删除硬编码双源漂移）；**B** worker.stop join 超时且线程存活不释锁不谎报（RuntimeError 如实暴露）、GC 移入 worker 取得 flock 后（`materials/gc.py`，第二实例锁前零触碰）、`_publish` 发布前复核 status/worker_id/cancel_requested（执行中取消→cancelled 不伪记 succeeded）；**C** 拒绝重复 slide_id、有效目标必须被内容页覆盖（纯封面宣称覆盖拒绝、封面无引用不误伤）、confirm CAS（`confirm_cas`，同载荷=重放/异载荷=拒绝，后写不覆盖）、删"按 created_at 猜最新确认计划"→`get_confirmed_plan_for_generation` 精确接缝（PLAN_NOT_CONFIRMED/CORPUS_CHANGED/PLAN_NOT_FOUND 精确分流）；**D** `idempotency_keys.operation_ref` 锚点（与 job 行同事务提交，"业务已提交响应未缓存"崩溃后同键重试恢复 202+原 job_id，api.md 幂等节同步）、`jobs.deadline_at` 总预算 600s（docs/06，plan/parse 受理即落，v1→v2 PRAGMA+ALTER 迁移保数据）、stage 真实上报（plan: retrieving→planning→validating；parse: parsing）+ 模型调用/入库前取消与 deadline 收口（共享 `jobs/execution_guard.py`，deadline 归 MODEL_TIMEOUT 与 adapter 语义一致）。文档：tasks.json T00–T08→DONE+evidence、validate_pack 任务断言适配为"状态诚实枚举+DONE 必须带证据"（严于原断言，沿 T03 校验器适配先例）、README/process.md 状态句同步。测试 389→**424**（A+8/B+3/C+6/D+15/Review 回归+3；并发用 Event/Barrier+临时 SQLite；崩溃注入在契约层）。门禁：`pytest backend/tests` **424 passed 退出码0**（连跑3次稳定）；`validate_pack.py` **23/23**；frontend `npm run build` 退出码0。独立 Review：1 阻塞（B1 带锚点占位被 60s 在飞窗口回收破坏恢复承诺——修 `_stale` 锚点豁免+>60s 恢复回归+24h 边界回归）+7 非阻塞（N2 set_stage running 守卫随修；N1/N3/N5/N6/N7 进 backlog），报告 docs/reviews/r00-review.md。诚实声明：真实模型 T08 大纲黄金链路 **NOT_RUN**（T07 smoke 授权范围=协议探针六项，不覆盖大纲生成调用）；e2e parse 等待窗 5s→10s（冷启动首跑 .pyc 编译+jieba 词典首载的异步收敛窗口，ready/succeeded 断言未放宽）。
- **T09-B 可信边界收口 + F00（2026-09-20，基线 6f58642，五循环 TDD 先红后绿）**：按 AGENT_00（Q01–Q08）+F00 收口 T09 剩余可信性边界。①循环①：独立可见文字审计 stage `audit_visible_text`+`VisibleTextAudit`+`app-prompts/audit.md` v1——零 claim 批不再因 claim_checks 为空自动盖绿（teaching/illustration/assumptions 专业事实必须入审）；豁免判据=教师计划 layout=title（模型自报 title 不豁免）；audited_slide_ids 与必审页集合服务端双射核验（遗漏/重复/未知页=整批审计无效 blocked）；missing_evidence 非空 P0 保守 blocked；verdicts 与预期 claim 集合严格双射（造新 ID=整批 not_checked，不采信"其余正常条目"）。②循环②：`ProposalSlide`/`ProposalIllustrationBlock` 提案/存储隔离（模型带 document_id/pdf_page/start/end 在 Schema 层直接拒绝；illustration refs 全部服务器 locator 解析，失败引用不入库）；`resolve_evidence` 必填 `allowed_chunk_ids`=本批实际进 prompt 集合（claim/illustration 两路径，项目存在≠模型可见）；**实锤修复：OUTPUT_SCHEMAS 缺注册 ContentProposal——真实 adapter 跑 generate 必抛 unknown schema，478 全绿期间从未被 Fake 之外的路径执行过（MockTransport 注册表测试补盲区）**；契约四文档同步（models.schema.json/openapi.json/samples/api.md 无 HTTP 变更）。③循环③：批内乱序=服务器按教师计划确定性重排（不浪费调用不乱序入库）；layout 门扩展每页 layout==计划 layout（偷改→blocked）；页增删换 id 门保持。④循环④：`guard_writable` 写候选前 DB 实况复查（running+未取消+deadline 未过）——取消/超时/interrupted 落在"最后核验完成→写库"窗口=JobCancelled/ModelTimeout 且 changes 零写入；ScriptedProvider 支持回调注入时序副作用。⑤循环⑤ F00：GET deck（缺省 current_version、0/缺失/跨项目 404 DECK_NOT_FOUND）+GET evidence（累积 revision、超前 409 CORPUS_CHANGED、新码 EVIDENCE_NOT_FOUND 404 入 api.md/error_mapping，response_model 契约化）+`courseware_api/dev.py`（三 handler 装配+唯一 worker+只绑 127.0.0.1）。纪律留痕：编辑工具曾两次错位污染测试脚本（partial/missing/fact_batch 用例），均读回核实修正——"引用测试结论前核对脚本实际内容"。测试 478→**524**（净增 46；红阶段逐循环确认旧实现必红：审计 10/提案 5+注册 1/顺序 3/写守卫 3/F00 12+dev 2+locator 1）。门禁：`pytest backend/tests` **524 passed 退出码0**；`validate_pack.py` **23/23**；frontend build 本轮未动（阶段二开工后验）。诚实声明：APP_LIVE=**NOT_RUN**（无真实模型预算授权；审计/核验语义遵从性只有 ScriptedProvider 结构背书，真实模型是否老实审计= T14 实测项，不拿 Fake 数字补）。独立 Review（干净会话子代理，huaweicloud/qwen3.8-flash）：**0 阻塞**+8 非阻塞，Q01–Q08/F00 逐项 CLOSED；当场闭环 N1（docs/05+docs/04 补 allowed_chunk_ids/提案隔离真值）、N2（api.md version<1→404 语义）、N5（重排 len 条件防去重失真）、N6（守卫反例 llm_calls 断言）、N7（openapi 换行）、N8（多批 missing 反例）；backlog：N3（648 行门外置）、N4（写库后→publish 前极窄竞态孤儿候选——彻底闭合需 job↔change 同事务/关联列，待负责人拍板）。
- **T11-F1 前端骨架施工（2026-09-20，基线 2fc36cd）**：AnyUI@0.5.2 正式接入（npm --save-exact，锁文件更新；入口 `@any-design/anyui/vue`、样式 `styles/index.css`；Vue 运行时 peers @iconify/vue/@popperjs/core 显式声明；--legacy-peer-deps 处理 react/svelte 多框架 peer 声明——T02 smoke 已验证路径，非 force 掩盖）；ADR-10 登记（docs/16）三业务视图替代 ADR-02 两路由；新增 CourseShell/stepNav、三页（Intake/Outline/Review）、StatusTag、api/resources.ts、types 全量契约对齐、tokens/base 样式、本地 favicon.svg、vite proxy CC_API_TARGET 覆盖（8000 被另一项目占用时开发可用，默认不变）。验证：`npm run build`（vue-tsc+vite）退出码 0；真实 dev 链（backend/.venv python -m courseware_api.dev @8010 + vite @5173 + 真实 POST /projects 创建项目）Playwright 截图 7 张归档 docs/screenshots/f1/；控制台无 JS 异常。诚实边界：未接写入操作（上传/确认/生成/commit/证据抽屉）全部 disabled+title 说明，生产路径无 mock；空态页面空白比例偏大属 F2/F3 数据填充后自然收敛项。测试：backend 524 不回归（本轮未动后端）。
- **T11-F2 真实资料→真实大纲施工（2026-09-20，基线 cc59a90）**：①F2-a 基础设施：`composables/useJobPolling.ts`（URL 驱动 jobId、前台 2s/后台 5s、终态停、卸载 abort、epoch 防旧回包、GET 失败绝不重发 POST、404/401 异常终态停止）、`api/errorMessages.ts`（api.md 错误码表 35 码→教师可读文案+未知码回落服务器 message+job.error 单独入口 describeJobFailure，绝不编造成功式安抚）、`api/idempotency.ts`（稳定键=操作意图+业务锚点派生、canonicalize 键序稳定序列化保证同载荷同键）。②F2-b `useMaterialIntake.ts`+IntakePage 接通：逐份上传循环（不用 Promise.all、等 parse job 释放写锁再下一份）、duplicate 无新 job 分支（等服务器活动任务）、job 写入 URL query+终态清除、刷新恢复（URL 精确 job 优先→服务器 active_job_id 兜底）、入库后队列项移除（服务器列表为唯一事实源）、PROJECT_BUSY/失败文案诚实呈现、创建键=表单指纹（结果未知重试同键命中恢复）。③F2-c `useOutlineFlow.ts`+OutlinePage 接通：标题/讲授重点编辑、上移下移、接受范围勾选（unsupported/conflict 目标禁勾；取消勾选前内容页引用预检=与后端"slides 不得触碰接受范围外"同规则）、"我已逐页审阅"勾选门（不能一次点击绕过人工确认）、**confirm 成功后才 POST generations 严格串行**（confirm 失败绝不进入生成）、已确认后重试只重发 generate 不新建计划、stale 计划只读、blocked 候选（job succeeded+result change）如实跳审阅页看报告不伪装失败。④**F2-d 发现的幂等设计缺口并修复**：纯业务锚点键会让失败 job 永久重放（教师修好配置后无法重跑，违背"结果未知才不换键"）——attempt 维度由 **URL query `pa` 承载**（与"URL 存精确定位 ID"设计一致，刷新/重开不丢）：job 终态=该键意图已消费，attempt+1 写回 URL；受理前抛错（结果未知）不走终态路径=同键恢复承诺保持。浏览器实测重放→换键→刷新→再换键全链闭合。⑤F2-d 真实浏览器链验收（**无模拟上传/解析**，1366×768，包内演示教材 demo-data/inputs 两 PDF）：创建 201→materials 路由；逐份上传 8 页/4 页真实解析 ready；第三份 duplicate 不占位（2/5）；job 写 URL→终态清除；生成大纲（无模型凭据）→job failed MODEL_PROTOCOL_ERROR→footer 显示映射文案+按钮恢复可重试；同键重放返回原 job（202，R00-D 恢复语义实测）；刷新恢复只读 brief+资料列表+就绪计数。截图 2 张归档 docs/screenshots/f2/。环境教训：/tmp 为 tmpfs 且长 hex ID 手抄失真造成 404 误判——dev 数据库迁至仓库外磁盘路径 .cc_demo/，ID 一律程序化取值比对（纪律再确认：结论引用前程序核对，不靠肉眼）。门禁：`npm run typecheck`/`npm run build` 退出码 0；backend `pytest` **524 passed 退出码 0**（本轮零后端改动）；`validate_pack.py` **23/23**。诚实边界：confirm→generations **成功链已 PASS**（负责人当轮授权 deepseek-flash，见下条 F2 成功链实测；此前凭据缺失阶段只验证失败路径+恢复语义）。独立 Review（干净会话子代理，审工作区 diff+契约真源）：3 阻塞（B1 轮询 404/401 停止不通知消费者→generating 卡死+文案与事实相反→onHalted 回调收口；B2 attempt 内存态刷新归零致首击重放旧 failed job→改 URL pa 承载+终态前进；B3 上传在飞跨项目切换 uploading 死锁+旧项目队列残留→restoreJob 复位+在途项降级 pending）+7 非阻塞（AbortError 闪现 pollError、PAYLOAD_TOO_LARGE 文案含数量超限、title 页引用豁免与后端 outside 检查不同口径、主按钮 disabled 无原因、Material 页数字段 schema 实为 nullable→类型对齐+入队按剩余额度截断、failed 队列项误阻断生成、错误码计数 36→35）**全部当场闭环**，报告 docs/reviews/t11-f2-review.md；修复后 typecheck/build/validate 复验全绿，B2 场景浏览器实测（重放→pa 前进→新 job→刷新→仍新 job）。
- **T11-F2 成功链真实模型验收（2026-09-20，负责人当轮授权 deepseek-flash 调用）**：APP_LLM_* 内存 export 重启 dev 后端。**真实调用抓到 3 个 ScriptedProvider 永远测不出的生产 bug**（prompt 与 Schema 字面错位，524 全绿期间未暴露）：①content v1 未给 kind 枚举字面值→模型输出 kind='fact'（22 条 literal_error）→v2 显式 direct/derived+术语否定；②content v2 仍缺 slides/blocks 结构形状→模型把 fact/teaching 平铺成 slide 顶层数组（9 条 missing/extra_forbidden）→v3 补 JSON 结构模板+layout 枚举全列；③verify v1 未给字段名→模型输出 "verdict"（schema 是 status，26 条错误）→v2 补结构+"不存在 verdict 字段"显式否定；audit 同根因预防性升 v2 补结构。修复严格 TDD（tests/unit/test_app_prompts_content.py 先红后绿，锁死字面量防退化），全量 **527 passed**、validate 23/23。**成功链实测**：大纲 job succeeded（llm_calls=1，8 页真实计划、EXTI 缺口诚实 unsupported 禁勾）→教师勾选审阅→confirm 200→generate succeeded（llm_calls=10，多批 generating→validating→finished）→自动跳转 review?change=精确 ID。候选真值核验报告：**42 claims 全 located、40 supported+2 partial、missing_evidence 8 条如实（不编造 EXTI/引脚/寄存器地址）、unbound 0、prompt_version="content-v3+verify-v2+audit-v2" 留痕、model_id=deepseek-flash**；relations 门全 supported 要求使 2 partial→can_commit=False→**候选诚实显示"未通过核验"、应用按钮禁用**（保守不盖绿=T09 设计行为）。截图 docs/screenshots/f2/（outline-real、review-real-blocked）。教训固化：prompt 契约字面量（枚举值/字段名/结构形状）必须写进 prompt 并有防退化测试——Fake provider 结构上不可能发现这类错位；Bash 工具每条命令独立 shell，跨命令 env 不保留，凭据 export 与启动必须同条命令。prompt 修复独立 Review（干净会话，逐字比对 4 份 Pydantic 真源+schema $defs）：**0 阻塞**+7 非阻塞，当场闭环 4 项实质（content 测试锁死 v3 全部结构字面键与 v4 边界数字、verify 测试锁死 status 四枚举带引号字面值、verify.md 样例 field_path 改 blocks[1].text 与"非 fact 块"规则自洽、content.md 升 v4 补 16 页/96 claims/evidence_refs≥1 边界声明）；不闭环项：文件命名（可接受）、rationale 表述严于 schema（方向安全仅记录）。**诚实边界**：真实模型成功验证发生在 content-v3+verify-v2+audit-v2；v4 仅增量边界声明（结构未变），未再消耗真实调用重验——prompt_version 留痕机制保证该差异可审计。测试 524→**530**（新增 6 条防退化锁），全量退出码 0；validate 23/23。
- 汇总：`smoke/t01_t02_probe_report.md`（环境矩阵、阻塞清单、给负责人的沙箱外命令清单、诚实声明）。
- 规划包静态检查以validation-report.md为准；未改远端。凭据 AK/SK 全程仅内存环境变量，未写入任何仓库文件。

## 收工记录模板

- 任务ID / commit / 改动路径。
- 实际完成内容及证据。
- 实际测试命令、退出码、失败和NOT_RUN。
- API/Schema/配置/提示版本变化。
- 新阻塞与费用/数据外发情况。
- 下一项唯一任务。

以后由施工AI每轮更新事实，不将这份初始说明持续当作当前状态。所有“计划已写”与“产品已实现”必须分开。
