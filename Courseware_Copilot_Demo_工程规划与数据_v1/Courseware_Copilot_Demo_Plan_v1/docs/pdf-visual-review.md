# PDF数据视觉检查记录

日期：2026-09-18。工具：ReportLab生成，PyMuPDF仅在本次制作环境渲染检查；随包重建工具采用ReportLab/pypdf/Pillow，不依赖PyMuPDF。

已把10份可直接读取的PDF共25页渲染成页面图，查看3张全页缩略总览；另外放大查看主讲义第5页，检查中文、行距、标题、依据说明、页脚。未发现正文截断、重叠或方框乱码。空白页为刻意负向输入；扫描页为刻意无文本层输入。加密与损坏输入不作为正常阅读文件渲染。

主讲义8页、案例4页、补充2页、变体4页；其余为负向文件。文本和物理页数另由validate_pack验证。图片只是本次数据PDF的检查，不是课程应用的PPT预览截图，不证明PPTX导出效果。

未进行OCR、未测试真实CodeArts、未调用应用模型、未生成宣称由应用产出的PPTX。源码、原文内容和物理页定位以包内数据为准。

## 离线阅读版附加检查

在Chromium中通过set_content载入最终HTML，查看1440×1000桌面首页与API章节，未发现截断或表格遮挡；记录JavaScript错误0、自动外部网络请求0。本容器策略阻止file://导航，因此不将该检查写成直接打开本地文件的成功记录。它只验证阅读版DOM与样式，不是Vue业务应用的浏览器验收。
